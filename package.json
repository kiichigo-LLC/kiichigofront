{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 1234",
    "build": "next build",
    "build:prod": "([ -f .env.local ] && mv .env.local .env.local.bak || true) && next build; s=$?; ([ -f .env.local.bak ] && mv .env.local.bak .env.local || true); exit $s",
    "start": "npx serve@latest out -l 1235",
    "lint": "eslint"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "next": "15.5.18"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.5.18",
    "@eslint/eslintrc": "^3"
  }
}
